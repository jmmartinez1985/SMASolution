@model IEnumerable<SMAWeb.Models.PA_Paises>

@{
    ViewBag.Title = "Index";
}


<br />
<div class="container">
    <div class="row-fluid margin-bottom-10">

        <div class="another-page">
            <div class="headline">
                <h3>Formulario de País</h3>
            </div>


            <p>
                @Html.ActionLink("Create New", "Create")
            </p>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.PA_Id)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.PA_Descripcion)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.REG_Region.REG_Descripcion)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.PA_Id)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.PA_Descripcion)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.REG_Region.REG_Descripcion)
                            </td>
                            <td>
                                @Html.ActionLink("Editar", "Edit", new { id = item.PA_Id }) |
            @Html.ActionLink("Detalles", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Eliminar", "Delete", new { /* id=item.PrimaryKey */ })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
