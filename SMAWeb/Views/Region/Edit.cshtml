@model SMAWeb.Models.REG_Region

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>REG_Region</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.REG_Id)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.REG_Id)
            @Html.ValidationMessageFor(model => model.REG_Id)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.REG_Descripcion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.REG_Descripcion)
            @Html.ValidationMessageFor(model => model.REG_Descripcion)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
