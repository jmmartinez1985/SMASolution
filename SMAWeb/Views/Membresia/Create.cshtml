@model SMAWeb.Models.MB_Membresia

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MB_Membresia</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.MP_MemberShipId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MP_MemberShipId)
            @Html.ValidationMessageFor(model => model.MP_MemberShipId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.MP_Descripcion)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MP_Descripcion)
            @Html.ValidationMessageFor(model => model.MP_Descripcion)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.MP_ExpiracionDays)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MP_ExpiracionDays)
            @Html.ValidationMessageFor(model => model.MP_ExpiracionDays)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
